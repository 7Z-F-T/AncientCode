#include "stdio.h"
#include "conio.h"
void main()
{
clrscr();
int a,c,x,result;
result=0;
for(a=0;a<=9;a++)
   { for(c=0;c<=9;c++)
      { result=a*1000+a*100+c*10+c;
       for(x=1;x<=99;x++)
       { if(result==x*x)
	  { printf("The x is%d\n",x);
	  printf("The number is%d\n",result);
	  }
       }
      }
   }
}



