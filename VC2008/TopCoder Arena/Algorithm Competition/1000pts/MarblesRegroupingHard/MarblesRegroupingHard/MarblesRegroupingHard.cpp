#include <iostream>
#include <vector>
#include <string>

class MarblesRegroupingHard
{
public:
	vector<int> selected(boxes[0].size());
	vector<int> used(boxes[0].size());
	int recursion(int row);
	int minMoves(vector <string> boxes)
	{
		for(int i=0;
	}
};